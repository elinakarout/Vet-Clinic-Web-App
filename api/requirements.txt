fastapi
uvicorn[standard]
pydantic
email-validator         # pydantic's EmailStr needs it; not bundled
pydantic-settings
sqlalchemy
alembic
pyjwt>=2.10,<3         # >=2.10 enforces a string `sub` claim; see PHASE_2.md
passlib[bcrypt]
bcrypt<4.1              # passlib 1.7.4's bcrypt backend breaks on 4.1+
python-multipart
anthropic
chromadb>=1.0,<2       # 1.x for configuration={"hnsw": ...}; see PHASE_6.md
# sentence-transformers  # NOT installed: chromadb bundles the same model
#                        # (all-MiniLM-L6-v2) as ONNX via onnxruntime, ~50 MB
#                        # of wheels instead of torch's 527 MB + nvidia deps
httpx
pytest
